package com.example.pinq_frontend.ui.theme

import androidx.compose.ui.graphics.Color

// ── FinQ Design System ──────────────────────────────────────────────────────
// Primary brand: deep navy + bright accent blue.

val FinQNavy = Color(0xFF1E2D6B)            // 풀블리드 네비
val FinQNavyDeep = Color(0xFF15214F)
val FinQNavyMid = Color(0xFF2A3F8F)
val FinQBlue = Color(0xFF2F58E0)            // 액센트 블루 (선택 / 정답)
val FinQBlueSoft = Color(0xFFE6ECFA)        // 블루 칩 배경

// Surfaces
val FinQBackground = Color(0xFFF2F4F8)      // 화면 배경
val FinQSurface = Color(0xFFFFFFFF)
val FinQSurfaceMuted = Color(0xFFF5F6FA)
val FinQDivider = Color(0xFFE5E7EE)
val FinQOutline = Color(0xFFD7DAE3)

// Text
val FinQTextStrong = Color(0xFF0F172A)
val FinQTextBody = Color(0xFF1F2433)
val FinQTextMuted = Color(0xFF6B7280)
val FinQTextSubtle = Color(0xFF94A3B8)

// Semantic
val FinQYellow = Color(0xFFFFC107)          // 별 / 강조
val FinQYellowSoft = Color(0xFFFFF5D6)      // 알아두면 좋아요 배경
val FinQYellowStroke = Color(0xFFE0A800)
val FinQGreen = Color(0xFF22C55E)
val FinQRed = Color(0xFFEF4444)
val FinQRedSoft = Color(0xFFFFE4E6)

val White = Color(0xFFFFFFFF)

// ── Backwards-compatible aliases (legacy names used across screens) ─────────
val PinQBlue = FinQBlue
val PinQDarkNavy = FinQNavy
val PinQBackground = FinQBackground
val PinQLightBlue = FinQBlueSoft
val PinQGreen = FinQGreen
val PinQRed = FinQRed
