package com.example.pinq_frontend.data.local

// Phase 4 변경: 오답노트가 서버 기반(UserQuizAttempt) 으로 이관됐다.
// 이전 SharedPreferences 기반 WrongNoteStore 는 더 이상 사용되지 않으며,
// 파일은 import 경로 충돌을 피하기 위해 의도적으로 비워뒀다 — 추후 안전하게 삭제 가능.
