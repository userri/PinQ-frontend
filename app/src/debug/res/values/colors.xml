<?xml version="1.0" encoding="utf-8"?>
<!--
  ⚠️ 디버그 전용 색. **app/src/main 으로 옮기지 말 것.**
  release 빌드는 app/src/debug 를 병합하지 않는다.

  **새 이름 하나뿐이다** — main 의 색을 아무것도 덮지 않는다. 이름을 덮으면 Showcase 가
  운영 아이콘을 렌더할 때도 이 색이 나와서 검증이 무의미해진다.
  앱 내부 색(lime 등)도 같은 이유로 건드리지 않는다 — 디버그로 본 화면이 운영 화면의
  대역 노릇을 해야 시각 검증이 성립한다. 갈라지는 것은 런처 배경 한 장뿐이다.
-->
<resources>
    <!-- 디버그 표식 앰버 — 라임과 명도는 비슷하되 색상환에서 멀어 축소돼도 갈린다. -->
    <color name="debug_accent">#FFFFB020</color>
</resources>
